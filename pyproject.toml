[project]
name = "sprite-station-studio"
version = "0.10.0rc1"
description = "Sprite creation studio powered by Blender and Unity workflows"
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"

[project.optional-dependencies]
ai = ["openai>=2.0"]
